# Build artifacts (regenerated by gprbuild / prove.sh via install.sh)
/obj/
/bin/
/lib/gnatprove/
/lib/*.so
*.o
*.ali
__pycache__/
*.pyc

# Local audit scratch (kept out of history; the dated report is committed intentionally)
