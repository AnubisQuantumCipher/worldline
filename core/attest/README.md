# Vendored ATTEST SHA-256

These three SPARK units are copied verbatim from the ATTEST evidence core
(`~/Projects/attest/src`, commit `unknown`), which has no public remote. They give the
kernel its proved SHA-256 and are re-proved with every proof gate run (they are part of the
130 checks). To refresh them run `core/attest/sync.sh`, which copies the files and rewrites
this note with the upstream commit; never edit them here.
